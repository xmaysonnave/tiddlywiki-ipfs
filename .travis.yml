language: node_js

os: linux
dist: focal
arch: amd64

node_js:
  - '15'

install:
  - sudo apt get zsh

after_success:
  - snyk monitor

script:
  - yarn build
  - yarn test

branches:
  only:
    - main
    - develop
